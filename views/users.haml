!!!
%html
	%head  
		%title Massr - Mini Wassr - User List.
		!= haml :head
	%body
		!= haml :header
		.container
			.row-fluid
				.span8
					%p
						=session[:errors]
					- users.each do |user|
						%p
							%img{:SRC => "#{get_icon_url(user)}" ,:width => "45" ,:height => "45"}
							= user.name
							= user.massr_id
							= user.created_at.localtime
				.span4
					!= haml :sidebar
			!= haml :footer
